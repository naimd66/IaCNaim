  GNU nano 7.2                   outputs.tf.save                            output "vm_ips" {
  value = [for vm in esxi_virtual_machine.vm : "${vm.name}: ${vm.ipv4_addre>}
1~output "vm_ips" {
  value = [for vm in esxi_virtual_machine.vm : "${vm.name}: ${vm.ipv4_addre>}
1~output "vm_ips" {
  value = [for vm in esxi_virtual_machine.vm : "${vm.name}: ${vm.ipv4_addre>}
1~output "vm_ips" {
  value = [for vm in esxi_virtual_machine.vm : "${vm.name}: ${vm.ipv4_addre>}
1~output "vm_ips" {
  value = [for vm in esxi_virtual_machine.vm : "${vm.name}: ${vm.ipv4_addre>}
1~output "vm_ips" {
  value = [for vm in esxi_virtual_machine.vm : "${vm.name}: ${vm.ipv4_addre>}